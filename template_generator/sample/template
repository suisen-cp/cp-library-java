public class ${CLASS_NAME} {
    public static void main(String[] args) {
        int ${VAR} = ${VAR_VALUE};
        int ${VARVAR} = ${VAR} * 2;
        int ${VARVARVAR} = ${VAR_VALUE} * 3;
        String dir = \"${DIR}\";
        String libPath = \"${LIBRARY_PATH}\";
    }
}